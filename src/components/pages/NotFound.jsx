import React from 'react';
import { Nav } from './../../common/Buttons';

const NotFound = (props) => {
	return (
		<section>
			<Nav />
			<p>Not Found</p>
		</section>

	)
}
export default NotFound;